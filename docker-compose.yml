version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    ports:
      - "80"
    depends_on:
      - php
    networks:
      - internal

  php:
    build:
      context: .
      dockerfile: Dockerfile.php
    environment:
      - DB_HOST=mariadb
      - DB_NAME=motaparfum
      - DB_USER=kmota
      - DB_PASS=Gucci1826
    networks:
      - internal
      - dokploy-network

networks:
  internal:
  dokploy-network:
    external: true
